import Editor from "./editor";

const Home = (): JSX.Element => (
  // <main className="w-screen h-screen bg-slate-300">
  <main className="w-screen h-screen bg-[#0E0E10]">
    <Editor />
  </main>
);

export default Home;
